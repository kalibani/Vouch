# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/

# Build output
dist/
build/
out/
coverage/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment & secrets
.env
.env.*
!.env.example
*.pem

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Misc
.cache/
tmp/
*.local

# Next.js / Vercel / build artifacts
.next/
next-env.d.ts
*.tsbuildinfo
.vercel

# Session handover scratch (resume aid, not a deliverable). Anchored to root so it
# does NOT match .claude/commands/handover.md (git is case-insensitive on macOS).
/HANDOVER.md

# Stray pnpm lockfile — this project uses npm (package-lock.json).
pnpm-lock.yaml
